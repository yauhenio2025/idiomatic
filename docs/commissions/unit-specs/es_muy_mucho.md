# `es_muy_mucho`

- Cluster: `9 Grado y cantidad`
- Bank: `es_muy_mucho.json` — 50 entries
- Format: F1 primary; F3 for all seven attested anchors
- Verification: Tier A closed inventory/form agreement plus Tier B blind-fill
- Recommended live size: 24 cards: 8 `muy`, 8 agreeing `mucho`, 4 `mucho más`, and 4 `tan/tanto`

## Generator guidance draft

Use neutral standard Spanish and one blank. `muy` modifies adjectives and adverbs; `mucho/mucha/muchos/muchas` quantifies nouns and agrees with them; adverbial `mucho` follows a verb and is invariable; `mucho más` intensifies a comparative; `tan` precedes an adjective/adverb in equal-degree/result structures; adverbial `tanto` follows a verb. Preserve the seven first frames as F3 anchors. Avoid lexical contexts where the category changes without enough context, and never accept `muy más`.

## Self-check

- JSON parsed; exactly 50 entries and one blank per frame.
- Answer distribution is 15 `muy`, 10 `mucho`, 5 each `mucha/muchos/muchas`, 4 `mucho más`, 3 `tan`, and 3 `tanto`.
- The first seven rows reproduce the learner's attested error directions.
- Capitalized sentence-initial `Muchos/Muchas` are intentional exact answers; the verifier should normalize case only when the blank begins the sentence.

