# Changelog

All notable changes to this project are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- **Anki estate reorg DECIDED (owner verdicts 2026-08-07)**: all six study recommendations accepted verbatim ([ANKI_ESTATE_OWNER_DECISIONS](research/ANKI_ESTATE_OWNER_DECISIONS.md) + `odd_decisions.approved.json`). Combined with the hub verdicts, every open decision is closed — next artifact is the migration execution runbook (Anki-side phases first, builder deck-name constants second, rollback journals throughout).
- **Expression-hub design DECIDED (user verdicts 2026-08-07)**: all four study recommendations accepted — six-example start with uncapped subordinate top-ups, Balanced weakness thresholds, dedicated Diagnosed-trouble-spots exercises subdeck for Flag-1 diagnoses, vertical-comic-rail hub card ([EXPRESSION_HUB_DECISIONS](research/EXPRESSION_HUB_DECISIONS.md) verdicts appended). Corpus-renderer two-insert fix: second insertions now explicitly additive (keep+don't-duplicate+don't-replace) after Mac images showed replacement AND duplication failures; pre-patch two-insert images queued for a repair pass on both machines.
- **Local TTS bridge BUILT + provider chain landed** (executing [LOCAL_TTS_BRIDGE_COMMISSION](commissions/LOCAL_TTS_BRIDGE_COMMISSION.md)): machine-local FastAPI bridge `~/llms/qwen3-tts/server/` (systemd user service `qwen-tts-bridge`, port 8355 behind Tailscale Funnel `https://fedora.tail363ee5.ts.net`) — bearer auth, lazy model load + 10-min idle VRAM unload, serialized generation with backpressure, etiquette gate (ComfyUI queue / GPU+CPU thermals / VRAM headroom; wait-don't-kill, only ComfyUI `/free`-when-idle + `ollama stop` remediations), −16 LUFS 24 kHz mono mp3 output parity, `/health` `/synth` `/synth-batch` `/unload`; frozen voice-continuity refs kit (5 approved ElevenLabs-clone refs + Sarah en ref via `/admin/tts-sample`, zh = native `vivian`). Server side: `qwen-local → elevenlabs → gemini` chain in `gemini.synthesize` ([idiomatic/gemini.py](../idiomatic/gemini.py)) with memoized ~60 s health probe (per-batch failover, no per-clip flapping), 503-defers-to-ElevenLabs, cost-0 `gen_ledger` rows, `eleven_voice_id` always bypassing the bridge; `qwen_tts_*` settings ([idiomatic/settings.py](../idiomatic/settings.py)); `/admin/tts-sample` now exercises the production chain unless an explicit voice forces ElevenLabs ([idiomatic/api.py](../idiomatic/api.py)); 9 routing tests ([tests/test_tts_routing.py](../tests/test_tts_routing.py), suite 385 green). **Default flip to qwen-local gated on the commission's acceptance drills** — Render env pins `TTS_PROVIDER=elevenlabs` until they pass; rollback stays `TTS_PROVIDER=elevenlabs`.
- **Qwen3-TTS approved as the future DEFAULT TTS provider (user verdict 2026-08-07)** after A/B bake-offs on real card audio — five-language ElevenLabs-voice CLONES + Mandarin HSK-3 natives, comparison pages under `/srv/ai-models/outputs/tts-samples/comparison/` (machine-local). ElevenLabs demotes to automatic fallback (box off/unreachable), Gemini stays last-resort. Build handed to a fresh session via [LOCAL_TTS_BRIDGE_COMMISSION](commissions/LOCAL_TTS_BRIDGE_COMMISSION.md): local FastAPI TTS service w/ lazy load + thermal/contention etiquette (wait-don't-kill), Tailscale-or-cloudflared bridge from Render, qwen-local→elevenlabs→gemini routing in gemini.synthesize, voice-continuity clone kit, acceptance incl. failover + etiquette drills. New `/admin/tts-sample` endpoint powers future bake-offs.
- **Expression-hub restructure LAUNCHED (user architecture session 2026-08-07)**: trigger expression = the unit of mastery; sentences/images/audio are projections. Two codex study commissions written for the user to run — [ANKI_ESTATE_REORG_COMMISSION](commissions/ANKI_ESTATE_REORG_COMMISSION.md) (language-first estate merge, per-video decks die, Pimsleur split + dormant demotion, scheduling sacred) and [EXPRESSION_HUB_MODEL_COMMISSION](commissions/EXPRESSION_HUB_MODEL_COMMISSION.md) (hub note per expression, fluency cards + hub card w/ image strip, Flag-1 protocol = non-trigger difficulty + LLM diagnosis queue, telemetry-driven example top-ups). **Idioms Audio pools DISCONTINUED** (`settings.build_audio_pools=False` — [pool.py](../idiomatic/pipeline/pool.py)); existing cards untouched. Illustration-prompt campaign fanning out (ES first) + first render batch started; images key on example_id and survive any restructure.
- **Rescue Comics deck v2 (apkg 1530, in-place card update)**: all 10 illustrations re-rendered per user verdicts — comic style (beats realism lane), NO bubbles (sentence lives in card text), **absurdity mandate** (Hokusai-wave fisherman, butterfly-dissolving deadline calendar, tear-flooded park, money tornado, office carried on a waiter's tray, snow-globe insider vs grey outsider), and a circular ref-photo medallion of the rendered actor composited in code on every card. Multi-sentence ontology decided: one ANCHOR hook+cast per expression, per-sentence VARIATION images (factory_assets.sentence_ref keying, 2-3/expression cap). Mac Studio node came online (ssh; full qwen stack pre-installed by the 08-04 night fetch); FLUX.2-Klein-4B + HiDream-O1 + Q-Judger downloads running there for separate experiments; s/image benchmark queued behind the bookscan pipeline's quiet window.
- **Rescue Comics deck v1 SHIPPED through the pipeline** (apkg 1528, kind `rescue_comics`, 10 notes / 4 lang subdecks): one illustration per struggling phrase (user rule), built from the day's fresh AnkiWeb pull — top-10 strugglers incl. *estar por dentro* (the approved v2 glass-pod strip), *andare per mare*, *folgenlos verstreichen* (Merkel + consequence-free calendar autumn), *seiner Verantwortung gerecht werden* (Neubauer as Atlas), *está tirado* (chalk-outlined empty coat; first roll drew a corpse — prompt lesson recorded), *sin freno*, *hacerse cargo* etc. **Semantic-hook rule validated and now standing: the setting stages the phrase's literal meaning** (inside/loop → cylindrical glass meeting pod). Cards: front = gloss + idiom-blanked failed sentence, back = idiom + sentence + illustration; famous cast inserted locally (Edit-2511, cloud sheets as identity refs); bubbles typeset. Machine-local builders: `~/llms/qwen-image/factory/{card_batch,build_rescue_comics_deck,strip_pilot2}.py`.
- **Cast v1 sheets COMPLETE on the cloud lane**: all 49 rendered via Qwen-Image 3.0 (~$2 total incl. remakes), synced to `/cast`; user-driven remake loop exercised end-to-end (Anitta ×2, Bracco→Melfi era, Van Zandt→Silvio era, Capital Bra + Moura from user-supplied photos — character-era refs beat present-day press photos for recognizability).
- **Cast Review panel** (`/cast` in the dashboard — new sanctioned mutation surface): fast-forward review of Asset Factory cast sheets — sheet + reference photo side by side, keyboard verdicts (O = ok, R = remake), per-actor notes, replacement-ref upload (auto-flags remake). Backend: `factory_actors` table (commission-B §7 subset incl. the approved-sheet-change→candidate demotion rule, [db/schema.sql](../db/schema.sql)), `/admin/factory/cast-upsert` (multipart, reusable for future enrollments), `/admin/factory/cast/{slug}/review`, `/admin/factory/cast/{slug}/ref`, read side `/ui/api/factory/cast` + authed image streamer ([idiomatic/api.py](../idiomatic/api.py), [idiomatic/ui_api.py](../idiomatic/ui_api.py)), SPA page [CastReview.tsx](../frontend/src/pages/CastReview.tsx). Laptop sync client `~/llms/qwen-image/factory/sync_cast_to_server.py` (machine-local). Suite 376 green.
- **Cloud sheet production run (Qwen-Image 3.0 pro)**: likeness pilot decisively beat the local Q8 lane (Falco recognizable on first attempt after 3 local failures; Cox/Merkel/Wagenknecht/Capital Bra/Rosalía all strong), prompt tightened for style coherence (flat ligne claire lock, no brand logos) after logo bleed-through (Gucci/Nike) and painterly drift; Capital Bra re-reffed from a user-supplied photo; full 49-face batch running at ~$0.037/sheet.
- **`POST /admin/genmedia-render`** ([idiomatic/api.py](../idiomatic/api.py)): raw cloud-image render for factory asset production — {provider, prompt, image_b64?, size?, model_override?} → PNG bytes + gen_ledger row (no item/asset; artifact lives on the render client). genmedia gains reference-image input on the DashScope and Ark adapters plus a model_override that never touches pricing ([idiomatic/genmedia.py](../idiomatic/genmedia.py)); 376 tests green. Motivation: cast sheets are one-time frozen assets reused by every insertion, so they go to the strongest available model (user directive) — and API keys stay server-side.
- **Famous-cast policy amendment (user verdict 2026-08-06)**: the "likeness renders never leave the laptop" clause is RESCINDED ([docs/ASSET_FACTORY_FAMOUS_CAST.md](ASSET_FACTORY_FAMOUS_CAST.md) §1.4) — user rejected it explicitly; cloud sheet generation via Qwen-Image 3.0 authorized. Private-study-only rule unchanged. Key status at the time: local DashScope key blocked, ARK key live but no Seedream image models activated, server DashScope key verified live.
- **Exclusion checker landed as a tool** ([tools/exclusion_check.py](../tools/exclusion_check.py)): the famous-cast doc §1.3 script now runs standalone (commission-B prerequisite). Capital Bra spot-check executed per handoff memo: OK under both stage name and real name (Vladislav Balovatsky); Fernando Haddad NEAR re-confirmed as the already-cleared "Fernando" token collision. Cast v1 is fully exclusion-verified — sheets unblocked.
- **Cast-sheet production started (machine-local, living-person policy — no likeness assets in git)**: Wikimedia reference photos fetched for the full v1 roster into `factory/refs/` (thumbnail API after 429s on originals); max-quality sheet runner (`~/llms/qwen-image/factory/render_sheets.py`, Edit-2511 no-LoRA 40 steps cfg 3.0, per-face signature-outfit prompts, anti-lineart negative prompt targeting the probe's color-drain failure) with idempotent skip/resume; Merkel pilot render validating the recipe before the batch.
- **Cast refs COMPLETE + recipe validated**: all 49 reference photos on disk and montage-audited (capital_bra re-sourced to a frontal studio still; 4 weaker refs flagged for possible re-source if the gate fails them); **Merkel pilot sheet PASSED recipe validation** — fully colored flat ligne claire, three views, all identity anchors (the probe's maxq color-drain failure is fixed by the negative prompt), ~7 min/face. Juju sheet also rendered. Remaining mini-batch (Klopp/Wagenknecht/Neubauer/Falco-retry/Cox) auto-resumes when the shared GPU frees up; full 48-face batch remains gated on the swapfile.
- **Wave 2 CONDITIONALS SHIPPED in all five languages** (apkgs 1517-1521): es 168 / pt 160 / fr 162 / de 163 / it 166 notes (2 cards each; 1,638 clips, 0 failures) — full conditional architectures per language (hubiera/habría, future subjunctive, si+imparfait, Konjunktiv II, congiuntivo trapassato); 15/15 chunks passed gate+audit; polluted tail items correctly triaged out. Exercises 2.0 now totals 1,772 notes / 3,544 cards.
- **Console verdicts round 2 executed** (all non-cast decisions resolved): telemetry system = next milestone; TENSES Wave 3 = raw-lapse order incl. literary tenses; podcast eps 2/10 approved to build; ES FALSE_FRIENDS added to armed purge; combined card back approved; 10 videos/mo escalation cap; rescue deck daily/comic-on-back; corpus-scale comics with model-judge QA (no per-item user approval); pt settings flavored for BOTH Brazil and Portugal; Mac Studio (M3 Ultra/96GB) to be integrated as second render node with a max-pregen push during the user's ~10-day absence.
- **Wave 2 (CONDITIONALS) LAUNCHED** per user verdict "Go now" (decision console): 15 chunks × 5 languages authoring via codex; audit → merge → build to follow.
- **Syllabus-only delivery policy** (user directive via decision console): the local add-on now hard-gates polling/import to evgeny@the-syllabus.com (`_IMPORT_PROFILES` allowlist; cleanup/reorganize hooks unchanged so the +2 purge can still fire); the +2 profile is legacy-only. Purge spec re-armed after a SECOND single-slot displacement (the 08-05 orphan job overwrote it) — queued cleanup jobs remain a factory commission-B requirement.
- **First cast verdicts recorded**: ES woman ~35 = Penélope Cruz (M survival → max-quality sheet + gate), ES man ~35 = Javier Bardem, ES young = Rosalía; ES woman ~70 slot returned "don't know any of them" — needs new candidates.
- **Asset Factory famous-cast & famous-places amendment** ([docs/ASSET_FACTORY_FAMOUS_CAST.md](ASSET_FACTORY_FAMOUS_CAST.md), commissioned, awaiting user review): mandarin-palace exclusion list extracted (60 anchors + 7-face likeness ring + 63 REDACT tells, reproducible checker script); recognizability probe run (6 local Edit-2511 renders, $0 — distinctive faces survive ligne-claire stylization AND insertion [Brian Cox PASS], smooth faces fail at 4-step [Edie Falco FAIL, max-quality promising] → per-face gate + survival-prior casting filter + max-quality sheet lane); ~50-face casting matrix (5 language casts + Sopranos/Succession/historical-pairs/correspondent wings, every candidate exclusion-checked); famous-places register for the 14 mined settings + weird-lane pool with a 1-in-6 weirdness budget (mixed-list bizarreness evidence); per-stage engine/likeness policy (likeness local-only, cloud likeness-free, video-rung de-brand ladder); schema deltas for commission B; amended user-decision list D1-D8. All likeness artifacts laptop-only per the living-person policy.
- **Asset Factory strategy** ([docs/ASSET_FACTORY_STRATEGY.md](ASSET_FACTORY_STRATEGY.md), commissioned investigation, reviewed): corpus-mined 14-setting/two-wing taxonomy (2,764 expressions, 16,548 sentences, live-DB evidence), 32-actor cast plan, registry schema extending idiomatic Postgres with a pull-based laptop render client (fixes Rescue's lease/join/delivered defects by construction), nightly cycle engine (~100 strips/night, $0), comics→video escalation strictly at strike 3 via panels-as-refs, combined comic+SVG card backs with a 7-archetype template library, 8 phased commissions A-H, 9 user decisions.
- **Tenses Rescue decks shipped (batch 1, all five languages)** ([idiomatic/grammar/tenses.py](../idiomatic/grammar/tenses.py), content [batch1.json](../idiomatic/grammar/data/tenses/batch1.json)): pilot approved same-day → top-3 verb×tense per language (fr swaps out the museum imparfait-du-subjonctif), 85 per-person drills as TWO rolling apkgs per lang — `tenses` (EN→form production, paradigm-at-a-glance back, fork notes, personal lapse history) + `tenses_ex` (fill-the-blank recycling the same sentences/audio). Sentences: 17 authored + 68 codex-generated (gated + audited). Build-time form verification against morphology tables / corpus-attested paradigms; Spanish gets its own ElevenLabs voice (George vetoed) with `/admin/tenses-voice-audition` for listen-and-pick. `/admin/tenses-build?lang`, `/admin/tenses-list`; 14 new tests (374 green).
- **Tenses profiles mined from the old account** ([docs/research/tenses-profiles/](research/tenses-profiles/), [tools/extract_tenses_profiles.py](../tools/extract_tenses_profiles.py)): the 2015-2022 `_tenses_old` conjugation corpus (14,267 cards / 176k reviews, fr/de/it/pt/es) extracted into per-language worst-verbs×tenses profiles + `tenses_priors.json` (attested paradigms + lapse stats) as a targeting prior, error-mine style. Findings: lügen·Präteritum = all-time worst card (108 lapses); the old schaffen card pair mixed the strong/weak paradigms; vir = worst verb overall (387 lapses over 12 tenses); liegen/legen confirm the error-mine fork. Grain caveat: source cards drilled whole paradigms, so per-person failure is unrecoverable — the **Tenses Rescue pilot №1** (per-person drill cards with paradigm-at-a-glance backs, fork notes, history strips; review artifact 🧭) awaits user verdict before any deck ships.
- **Rescue Lab SHIPPED** (per [RESCUE_LAB_COMMISSION.md](commissions/RESCUE_LAB_COMMISSION.md)): the rescue initiative's operating surface — `rescue_items`/`rescue_assets`/`rescue_senses`/`gen_ledger` schema ([db/schema.sql](../db/schema.sql)); switchable image-provider registry ([idiomatic/genmedia.py](../idiomatic/genmedia.py): nano-banana $0.067/img, nano-banana-lite $0.0336/img — verified against official pricing docs, correcting the commission draft's stale 0.039/0.02); format taxonomy + pilot-seeded prompt templates with hard rules in code ([idiomatic/rescue.py](../idiomatic/rescue.py): polysemy teach-every-door approval guard, anatomy strict letter-order demand, glyph permanence, NO video anywhere); admin endpoints `/admin/rescue/*` (struggles upload, generate→draft asset+ledger, verdict, item patch incl. replace-all senses, export bundle); read-only `/ui/api/rescue/*` (items, item detail with server-filled prompts, cost aggregates, formats, authed asset streamer); dashboard **Rescue Lab** section ([RescueLab](../frontend/src/pages/RescueLab.tsx)/[RescueItem](../frontend/src/pages/RescueItem.tsx)/[RescueFormats](../frontend/src/pages/RescueFormats.tsx) — cost tiles, senses editor, side-by-side asset compare with approve/reject, Generate panel showing estimated cost before the call); pilot cohort seeder ([tools/seed_rescue_pilot.py](../tools/seed_rescue_pilot.py)); 23 deterministic tests incl. a schema round-trip on an ephemeral local Postgres ([tests/test_rescue.py](../tests/test_rescue.py), 351 total green). Verified end-to-end locally with Playwright before deploy.
- **Rescue Lab commissioned** ([docs/commissions/RESCUE_LAB_COMMISSION.md](commissions/RESCUE_LAB_COMMISSION.md)): round-2 formats approved by the user ("brilliant — incorporate them"); commission drafted for a fresh session to build the operating dashboard — rescue_items/assets/senses/gen_ledger schema, switchable image-model registry (genmedia.py, Gemini-family only, video excluded), admin generate/verdict endpoints, /rescue SPA route with cost accounting. Polysemy design rule locked in (every sense taught with gloss + micro-example); pilot source JSONs preserved in [docs/research/rescue_pilot1/](research/rescue_pilot1/).
- **Rescue Pilot round 2** ([docs/research/RESCUE_PILOT.md](research/RESCUE_PILOT.md)): round-1 verdict recorded (comics > diagrams > videos dropped); five word-centered image formats designed + exemplars generated (inside/outside contrast, polysemy map, morphology anatomy, iconic poster, idiom glyphs) and the repeat-failure escalation ladder specified (diagnose failure type from revlog grain, switch encoding axis per strike, glyph as constant identity, auto-retire on recovery). Review artifact extended in place.
- **Idiom Rescue Pilot №1 generated** ([docs/research/RESCUE_PILOT.md](research/RESCUE_PILOT.md)): first personalization experiment off the study-data POC — the 9 idioms the user failed in the 2026-08-05 morning session (3 per lang es/pt/it) each get an anchor mnemonic, 3 personalized sentences with main-deck ElevenLabs voices, 2 exercises, and a per-language hero-format triad (MiniMax-H3 video / Nano Banana comic / authored SVG diagram). Assets generated; review artifact for approval; nothing ships until approved (pilot-first).
- **Desktop Anki fully hands-off**: the idiomatic_puller add-on (local) now syncs with AnkiWeb programmatically — after every import batch and every 30 min via `mw._sync_collection_and_media`, gated on `sync_status` so a full-sync conflict is never auto-answered (it nags instead); new Tools → "Idiomatic: Sync now" menu item; Anki autostarts at login (`~/.config/autostart/net.ankiweb.Anki.desktop`). No more manual open/close to move reviews/decks between iPad and server.
- **Orphan reconciliation executed** ([idiomatic/pipeline/adoption.py](../idiomatic/pipeline/adoption.py), `GET /admin/anki-guids`, `POST /admin/adopt-orphans`, `adopted_notes` table in [db/schema.sql](../db/schema.sql)): censused 33,012 YT-pipeline notes in the active collection against the live catalog → 8,217 orphans; per user directive the 4,752 studied ones (16,891 reviews) were adopted into `adopted_notes` (full fields + study stats) and the 3,465 never-studied ones deleted via cleanup.json (verified: 0 remain, all adopted notes intact).
- **Anki study-data POC — verified working** ([docs/research/ANKI_STATS_POC.md](research/ANKI_STATS_POC.md)): headless download-only AnkiWeb sync (stored `syncKey` from local `prefs21.db` + the `anki` pip library) delivers the full iPad `revlog` — per-review button, time-on-card, interval, language — 40 minutes after the study session; note guids recompute byte-for-byte from pool.py's `_guid` recipe, mapping struggling cards to live `expressions` rows (with orphan-card and renamed-deck caveats documented). Research only; no pipeline code yet.
- **Wave 1 complete in five languages**: IT CONNECTING authored from the rebuilt corpus (4 codex chunks, 201 notes, apkg 1396, 402 cards) — Exercises 2.0 CONNECTING now totals 953 notes / 1,906 cards; translation decks rebuilt with sentence-only back audio per user feedback (apkgs 1390-1394, 100% cache reuse); roadmap of record in [docs/EXERCISES2_ROADMAP.md](EXERCISES2_ROADMAP.md); batch gate learns it/pt elision false-positive suppression.
- **Mandarin sentence-reading exploration report** ([docs/research/MANDARIN_SENTENCE_READING.md](research/MANDARIN_SENTENCE_READING.md), per [MANDARIN_SENTENCE_READING_COMMISSION.md](commissions/MANDARIN_SENTENCE_READING_COMMISSION.md)): Phase-0 audit of the Memory Palace assets (5,138 course sentences, 3,050 composed character films, 19,975-word bank) + computed i+1 feasibility against the LingQ zh mirror and palace evidence — 99.8% of sentences fully char-decodable, 1,882-sentence i+1 sweet spot (885 pilot-ready) — with three ranked method proposals and a pilot spec ("Sentence-Walk" podcast-card lessons with film-frame mnemonic quotes). Report only; pilot awaits user approval.
- **Translation-exercise decks live in all five languages** (apkgs 1357-1361: es 250, fr 146, it 130, pt 127, de 79 cards): verified grammar drills repurposed as EN(voiced)→TL(existing ElevenLabs clip) translation cards with the drilled form bolded — zero new TL synthesis, 732 EN narrator clips, 0 failures ([idiomatic/grammar/translation.py](../idiomatic/grammar/translation.py), codex-built per [TRANSLATION_DECKS_COMMISSION.md](commissions/TRANSLATION_DECKS_COMMISSION.md), reviewed+merged).
- **PT Conectores shipped** (apkg 1355, 175 notes/350 cards): metalanguage normalized to English per user decision (frozen fields verified byte-identical); CONNECTING campaign now live in all four commissioned languages — 752 notes / 1,504 cards total.
- **CONDITIONALS front prepared**: 15 chunk inputs (five languages — IT joins via its rebuilt corpus), sentence-level categories in the exercises2 schema, [addendum commission](commissions/EXERCISES2_CONDITIONALS_ADDENDUM.md) (tl=sentence, NEW parallel example, cloze on governed verb pairs).
- **Old-account purge armed**: add-on gained a profile-guard for cleanup.json (skips without consuming under the wrong profile); purge spec targets the 12 fake-Italian decks + 30 Spanish-backed PT notes, fires on next open of the old profile.

### Fixed
- **Rescue-autopilot double-run race closed** ([idiomatic/db.py](../idiomatic/db.py) `kv_claim_interval`, [idiomatic/rescue_autopilot.py](../idiomatic/rescue_autopilot.py)): `maybe_run_autopilot`'s read-then-check on the last-run stamp let two overlapping workers (deploy overlap/restart) both start a generation run and double-spend; replaced with a single atomic check-and-stamp upsert (loser's WHERE re-evaluates against the winner's committed value; garbled legacy stamps never wedge the slot). Concurrency-burst test on ephemeral Postgres — exactly one of 8 simultaneous claimants wins (375 green). Handoff-memo task 8; the full lease machinery remains commission B.
- **ENOSPC incident** ([docs/incidents/2026-08-04-enospc.md](incidents/2026-08-04-enospc.md)): orphaned per-video apkg files (purge-video deletes the DB row → row-driven reaper blind) accumulated ~6 weeks; janitor gained an orphan sweep, `apkg_retention_days` 30→12, new `/admin/disk-usage` endpoint (tree + per-file drill-down). All six failed builds re-ran clean.

- **Translation-exercise decks from grammar drills** ([idiomatic/grammar/translation.py](../idiomatic/grammar/translation.py), per [TRANSLATION_DECKS_COMMISSION.md](commissions/TRANSLATION_DECKS_COMMISSION.md)): verified drill sentences become EN→TL translation cards — FRONT speaks `gloss_en` via new cached leveled EN TTS (`staged_audio/grammar/translation_en/<lang>/`), BACK reuses the drill's existing back-audio clip with the drilled form bolded (reuse-only: no TL synthesis ever; clip-less items skipped + counted). New frozen 14-field `Idiomatic Translation v1` model (one Translate template), `Idiomatic Translation {LANG}::{cluster}` subdecks sharing the grammar cluster strings, rolling `apkgs.kind='translation'` per lang (kind added to `db.upsert_pool_apkg` + schema comment), zero add-on changes. `POST /admin/translation-build?lang`, `GET /admin/translation-list` ([idiomatic/api.py](../idiomatic/api.py)); 16 deterministic tests in [tests/test_translation.py](../tests/test_translation.py) (327 total green).
- **Exercises 2.0 delivery wiring** ([idiomatic/grammar/exercises2.py](../idiomatic/grammar/exercises2.py)): format approved off the HTML review artifact 2026-08-04 — reviewed notes JSON (`data/exercises2/notes/<lang>_<topic>.json`; pilot moved to [es_connecting.json](../idiomatic/grammar/data/exercises2/notes/es_connecting.json)) is validated (schema, category whitelist, cloze↔example agreement, dupe ids), given two cached −16 LUFS ElevenLabs clips per note (answer + example; silence-marked failures ship text-only), and packaged as a new frozen 17-field `Idiomatic Exercises v1` model with Production + Cloze templates into `Idiomatic Exercises {LANG}::{Topic}` subdecks; rolling `apkgs.kind='exercises2'` per language (kind added to `db.upsert_pool_apkg` + schema comment) delivered with zero add-on changes. `POST /admin/exercises2-build?lang`, `GET /admin/exercises2-list` ([idiomatic/api.py](../idiomatic/api.py)); 23 deterministic tests in [tests/test_exercises2.py](../tests/test_exercises2.py) (311 total green).
- **CONNECTING campaign COMPLETE (es/fr/de shipped, pt parked)** — user-run parallel codex sessions delivered 15 of 16 chunks (100% mechanical-gate pass, zero linguistic errors found in stratified sampling; sessions self-revise landed files, so merges are idempotent rebuilds from latest outputs). Final decks: ES Conectores 207 notes/414 cards (apkg 1337), FR Connecteurs 191/382 (apkg 1340), DE Konnektoren 179/358 (apkg 1352); PT 175 notes audit-cleared, parked on the metalanguage decision. 16/16 chunks delivered by the user's sessions (DE b04 arrived before the codex fallback fired); ~half of 1,562 legacy inputs kept with per-item triage verdicts. Gate false-positive noted (ptc283 — Portuguese circumflexes tripping the FR regex).
- **Wave-1 CONNECTING batches audited + first multi-language decks live**: user-run codex sessions delivered `{es,fr,pt}_connecting_b01`; all passed the mechanical gate ([tools/x2_batch_gate.py](../tools/x2_batch_gate.py)) and the linguistic audit (every tracked defective legacy gloss caught — De inmediato, sans délai/sur ce, para fins de, etc.). ES merged to 92 notes (apkg 1324, 184 cards), FR debuted with 54 notes (apkg 1325, 108 cards); PT b01 approved on content but held pending a metalanguage decision (its register/trap lines are in Portuguese vs the pilot's English convention).
- **Italian corpus rebuild via codex** — COMPLETE 2026-08-04: all 27 chunks delivered ([output](../idiomatic/grammar/data/exercises2/it_rebuild/output/)); gate found 0 French items, 0 structural problems across 2,589 renderings (1 legitimate FR-identical cognate, "Perspicace"); 48-sample hand audit across 13 chunks found 0 errors; 362 honest self-review notes.
- **(superseded by the entry above) Italian corpus rebuild via codex** ([commission](commissions/EXERCISES2_IT_REBUILD_COMMISSION.md), [inputs](../idiomatic/grammar/data/exercises2/it_rebuild/input/), [driver](../tools/it_rebuild_driver.sh)): the fake-French Italian deck's 2,589 deduplicated EN prompts (11 topics) packaged with es/fr/pt/de reference translations (the 30 Spanish-contaminated PT refs excluded) into 27 chunks; per-chunk codex runs with a mandatory anti-French verification pass, 3-parallel resumable driver.
- **Exercises 2.0 pilot content, codex-generated + audited** ([input](../idiomatic/grammar/data/exercises2/es_connecting_pilot_input.json), [output](../idiomatic/grammar/data/exercises2/es_connecting_pilot.json), [preview](../idiomatic/grammar/data/exercises2/es_connecting_pilot_preview.md)): 42 ES CONNECTING rich notes (main + alternatives, register, interference trap, in-register example, cloze) produced by `codex exec` from [EXERCISES2_PILOT_COMMISSION.md](commissions/EXERCISES2_PILOT_COMMISSION.md); delegation experiment result — codex caught all 5 seeded legacy-gloss traps plus 6 more defects unprompted, zero outright errors found in review; awaiting user approval of the format before batching.
- **Legacy EXCERCISES deck audit** ([docs/research/legacy-excercises-audit.md](research/legacy-excercises-audit.md)): full audit of the 13,377-card 2023-era EN→TL exercise tree in the old Anki account — the "Italian" deck is 2,612/2,612 byte-identical to the French deck (never generated), 30/91 PT BIG_TECH_PHRASES backs are Spanish, ES FALSE_FRIENDS is ~half factually wrong; usefulness ranking + "Exercises 2.0" revival proposal (regenerate from the shared English prompt set through the grammar-initiative machinery, pilot-first).
- **Podcast cards episode 9 (pt, "Futuro do subjuntivo")** ([source](../idiomatic/grammar/data/podcast_cards/pt_future-subjunctive.md) + 10 SVG diagrams): the future-subjunctive season episode as 5 two-sided cards — the recorded quando-eu-vou-voltar error as the spine, the stem machine (past-plural minus -am), pending-not-unlikely gate, three temporal statuses, open conditions + the whether trap, open relatives, the antes-que/caso boundary; 43/45 TL lines season-verbatim. **Completes the seven-episode batch (PODCAST_CARDS_BATCH_COMMISSION.md).**
- **Podcast cards episode 8 (it, "Plurali bugiardi")** ([source](../idiomatic/grammar/data/podcast_cards/it_deceptive-plurals-and-genders.md) + 10 SVG diagrams): the deceptive-plurals season episode as 5 two-sided cards — the recorded città-intelligenti error as the spine, regular defaults, three-part noun packages, the -ma family, reverse traps + invariants, contraction signals, gender-changers and meaning-selected body plurals; 48/48 TL lines season-verbatim.
- **Podcast cards episode 7 (it, "Il congiuntivo")** ([source](../idiomatic/grammar/data/podcast_cards/it_congiuntivo.md) + 10 SVG diagrams): the anti-avoidance congiuntivo season episode as 5 two-sided cards — stance vs assertion, the vowel switch + sound anchors, frame architecture with the fact-frame boundary, same-subject compression, opinion/secondo-me contrast, sought-vs-known incl. the teacher's riesca target, se-guardrail; 37/48 TL lines season-verbatim.
- **Podcast cards episode 6 (de, "Adjektivendungen")** ([source](../idiomatic/grammar/data/podcast_cards/de_adjective-endings.md) + 10 SVG diagrams): the adjective-endings season episode as 5 two-sided cards — the decision route, weak/mixed/strong as one signaling job, the mixed-family holes behind the recorded fossils, the two-ending weak table, strong-branch article-borrowing, predicate boundary; 56/57 TL lines season-verbatim.
- **Podcast cards episode 5 (de, "Die Fälle")** ([source](../idiomatic/grammar/data/podcast_cards/de_case-system.md) + 10 SVG diagrams): the case-system season episode as 5 two-sided cards — the three-person scene, der/den/dem/des landmark, three gates (structure, verb government incl. the versorgen frame, prepositions + two-way), give-and-tell rhythm with the recorded errors, gender-trap quick fire; 49/55 TL lines season-verbatim.
- **Podcast cards episode 1 (es, "El subjuntivo")** ([source](../idiomatic/grammar/data/podcast_cards/es_subjunctive-machine.md) + 10 SVG diagrams): the subjunctive-machine season episode as 5 two-sided cards — stance vs assertion, the vowel switch, influence family + compression rule, reaction/doubt with the no-dudo trap, pending time/unidentified antecedent/si-firewall, quick fire; second of the approved batch, 33/45 TL lines season-verbatim.
- **Podcast cards episode 4 (fr, "Le genre")** ([source](../idiomatic/grammar/data/podcast_cards/fr_gender-is-memory.md) + [10 SVG diagrams](../idiomatic/grammar/data/podcast_cards/svg/)): the gender-is-memory season episode restructured into 5 two-sided cards per the approved ep-3 format — packet principle, final-E trap, l'/mon disguises, suffix compressors + head-noun rule, quick-fire — first of the seven-episode batch (episode-4 checkpoint per [PODCAST_CARDS_BATCH_COMMISSION.md](commissions/PODCAST_CARDS_BATCH_COMMISSION.md)); 49/54 TL lines verbatim from the season script (TTS cache-shared).
- **Podcast cards pilot (episode 3, fr)** ([idiomatic/grammar/podcast_cards.py](../idiomatic/grammar/podcast_cards.py)): grammar-walk episodes as ~5 two-sided Anki notes — per-side narration through the shared explainer clip cache, rich HTML ([CARD]/[SIDE]/TITLE:/SHOW:/TL-: markup, [source](../idiomatic/grammar/data/podcast_cards/fr_quantity-system.md)), and one visual per side. New frozen `Idiomatic Podcast Lesson v1` model (14 fields, slug+seq GUIDs); delivery via `apkgs.kind='podcast_lesson'` per lang into `…::0 Écoute::03 Beaucoup de` subdecks, zero add-on changes; `POST /admin/podcast-cards-build?lang&episode`, `GET /admin/podcast-cards-list`; `gemini.generate_image` + `gemini_image_model` setting. PILOT format approved by user 2026-08-03 after two review rounds; batching episodes 1+4-9 is the next step.
- **Podcast-card visuals are authored SVG diagrams** ([svg sidecars](../idiomatic/grammar/data/podcast_cards/svg/)): pilot review showed generated images drift semantically (Gemini read "de" as a language code), so the `SVG:` markup line inlines hand-authored, sanitized SVG into the card fields — night-mode themed via a shared `s-*` palette in the model CSS, no media files, no JS, $0/episode. `IMG:` Nano-Banana generation remains available per side for mood scenes.

### Fixed
- Stale exact-count assertion in [tests/test_exercises2.py](../tests/test_exercises2.py) (`test_real_pilot_file_parses`): the ES CONNECTING campaign grew `es_connecting.json` from 42 to 207 notes (commit f72ce22) without updating the test, leaving the suite red; assertion updated to 207.
- Uneven loudness between the English narrator and target-language voices in all stitched lessons (French examples were much quieter): the shared explainer renderer now levels every speech clip to −16 LUFS before stitching (`leveled_speech_clip` in [idiomatic/grammar/explainers.py](../idiomatic/grammar/explainers.py) — static gain + limiter, ebur128 with volumedetect fallback for one-word clips, cached beside the raw clip; renderer revision bumped so every rebuild re-stitches). The global loudnorm in `concat_mp3s` could never fix imbalance inside one file. Applies to podcast lesson cards, season MP3s, and explainer cards on their next rebuild.

### Changed
- Podcast-card ep3 rev2 after user review: back sides gained spoken next-card cues; practice items follow a fixed prompt→music→answer→French-echo→English-tip pattern; all multi-word French moved off the English narrator voice ([source](../idiomatic/grammar/data/podcast_cards/fr_quantity-system.md)).
- Grammar-walks season 1 delivered ([idiomatic/grammar/podcasts.py](../idiomatic/grammar/podcasts.py)): 8 episodes (13-15 min each, ~115 min total) rendered via the explainer TTS pipeline to numbered MP3s under staged_audio/grammar/podcasts/, streamable through the dashboard audio route; `/admin/podcasts-build|list`. Cross-language episodes 2 and 10 pending per-line TL language markers.
- **Wave 7 build-out (codex commissions C-H, reviewed + merged)**: F3 error-correction format ([idiomatic/grammar/f3.py](../idiomatic/grammar/f3.py)) — teacher-attested personal errors become cards in per-language "9 my errors" clusters via `/admin/f3-convert`; F4 cross-language interference ([idiomatic/grammar/f4.py](../idiomatic/grammar/f4.py)) — private reviewed banks travel through DB staging/cron ingest and compile deterministically into four receiving-language "10 … interference" clusters via `/admin/f4-convert`, with the frozen model and GUID formula untouched; ten bank-backed units use deterministic/blind verification; and 12 grammar-explainer scripts ([idiomatic/grammar/data/explainers/](../idiomatic/grammar/data/explainers/)) have a delivery design. The F4 banks, direction matrix, and vocabulary mine remain outside the public repo in `~/projects/idiomatic-data`. Curriculum: 61 active + 6 planned units.
- Personal-error registry loading, cron-contained ([idiomatic/personal_errors.py](../idiomatic/personal_errors.py)): `personal_errors` + `personal_errors_staging` tables; `/admin/personal-errors-upload` validates the commission-A JSONL and does ONE staging blob insert; the cron container batch-upserts on its next tick (web process never bulk-writes — LingQ containment lesson; cron cannot see /data, so the DB is the handoff); `/admin/personal-errors-status` for progress. Unit-bank open questions resolved in [docs/commissions/unit-specs/README.md](commissions/unit-specs/README.md) (Italian cluster strings final; de_dativ_verben ships on blind-fill, NP-inflection engine bundled into de_adj_endings). Grammar unit pages now show the LingQ vocabulary sample woven into generation ([frontend/src/pages/GrammarUnit.tsx](../frontend/src/pages/GrammarUnit.tsx)).
- **LingQ vocabulary mirror** ([idiomatic/lingq.py](../idiomatic/lingq.py)): the user's ~52k saved LingQs across 10 languages sync into `lingq_terms` (token in DB kv_store, never the public repo; daily cron auto-sync); grammar generation now offers a fresh per-topic sample of studied vocabulary as optional sentence material (never the blank); `/admin/lingq-token|sync|status|sample` — the sample endpoint is the access path for local agents building exercises.
- Error-mine synthesis + leverage proposal ([docs/commissions/ERROR_PROFILE_PROPOSAL.md](commissions/ERROR_PROFILE_PROPOSAL.md)): cross-language deficiency profile from the five error-profile reports (selection/agreement over conjugation; interference #1 with zero coverage; named fossils; refuted units), phased Wave-7 plan (target retuning, personal_errors registry + F3 format, evidence-based new units, F4 interference deck, error-aware generation, grammar explainers), and a ready-to-run deep-study commission.
- Learner error profile for French ([docs/research/error-profiles/fr.md](research/error-profiles/fr.md)): mined 3,925 xlsx rows (≈2019-2022) + 1,350 Teachee notes (2022-2024); taxonomy over 2,523 use-flagged errors (prepositions 25%, gender/agreement 16%, articles/quantifiers 9% — verb-tense selection only ~2%), 25 fossilization patterns (beaucoup des 138×, an/année 102×, NUM+dernier order ~90×, en+city 36×), pronunciation + vocab interference profiles, curriculum mapping (promote fr_pronoms_y_en; 12 proposed new units in clusters 5-9), 40-pair verbatim F3 seed JSON.
- Learner error profile for Portuguese ([docs/research/error-profiles/pt.md](research/error-profiles/pt.md)): mined 4,521 xlsx rows (2019-2022, 1,098 explicit error pairs) + 1,400 Teachee notes (2023-2024); taxonomy of 19 categories — Spanish/Italian interference 54%, gender/articles 18% (the -ma/-agem/dois-duas fossil core, still remediated in 2023-24), verb+prep calques (tentar de, conseguir a, vou a), 1sg↔3sg pretérito swaps, ser/estar/ficar, future-subjunctive avoidance; 24 fossilization patterns; curriculum mapping (raise pt_preterito_perfeito + pt_futuro_subjuntivo, activate pt_clitic_placement, lower futuro/condicional; new proposed units pt_gender_core, pt_regencia_verbal, pt_ser_estar_ficar, pt_es_contrastes); 40-pair F3 seed JSON.
- Learner error profile for German ([docs/research/error-profiles/de.md](research/error-profiles/de.md)): 346 xlsx rows (9 lessons, April 2019) + 379 Teachee notes (2022–2024) mined; taxonomy of the 149 use-flagged corrections, ~28 verbatim retained errors — adjective endings/gender/case dominate and recur across both eras; verdict: build de_adj_endings first, then passive (35 teacher-supplied instances); proposes a "5 Kasus" cluster (dative verbs, n-declension, genitive) and ships a 30-pair F3 seed list.
- Learner error profile for Spanish ([docs/research/error-profiles/es.md](research/error-profiles/es.md)): mined 1,389 xlsx rows (2019-2022) + 304 Teachee notes (2022-2024); 62 verbatim error pairs categorized (cross-Romance interference 42%, muy/mucho, motion prepositions, strong preterites), 20 fossilization patterns, curriculum mapping over the 18 active es units (raise es_preterito/es_clitics_selo/es_verb_prep, new proposed units es_interferencia/es_muy_mucho/es_light_verbs/es_numeros_fechas), 40-pair F3 seed JSON.
- Learner error profile for Italian ([docs/research/error-profiles/it.md](research/error-profiles/it.md)): mined the 2019 xlsx/Anki `_it_errors` data (133 rows, only 11 recorded production errors), taxonomy + F3 seed pairs + curriculum mapping; documents the Teachee gap and proposes a live error-capture path via the add-on.
- **Wave 6 — grammar subdecks + dashboard grammar section** ([commission](commissions/GRAMMAR_FRONTEND_COMMISSION.md)): grammar apkgs now build one Anki subdeck per topic cluster (`Idiomatic Grammar {LANG}::{cluster}`, [idiomatic/grammar/apkg.py](../idiomatic/grammar/apkg.py)); `grammar_units` table (cluster/label/symbol re-seeded from curriculum code on boot, status/target_size/notes DB-mutable, `planned` units listed for what's-next); dashboard **Grammar** pages `/grammar` + `/grammar/unit/:key` ([frontend/src/pages/Grammar.tsx](../frontend/src/pages/Grammar.tsx), [GrammarUnit.tsx](../frontend/src/pages/GrammarUnit.tsx)) with Top-up/Rebuild/retire/unit-edit controls; new endpoints `/ui/api/grammar/*`, `/admin/grammar-deckmap` (agent-authed), `/admin/grammar-unit/{key}`, `/admin/grammar-topup/{key}`, `/admin/grammar-retire-item/{id}`; add-on gained a one-shot "Reorganize grammar decks" migration (local, `col.set_deck` by unit tag — review history preserved).
- Commission brief for the dashboard grammar section + subdeck taxonomy ([docs/commissions/GRAMMAR_FRONTEND_COMMISSION.md](commissions/GRAMMAR_FRONTEND_COMMISSION.md)); Wave 6 added to the plan of record.
- Wave plan (grammar roadmap of record) in [docs/GRAMMAR_STRATEGY.md](GRAMMAR_STRATEGY.md) §8, plus codex-delegation working rule (§8b, CLAUDE.md).
- Grammar drill pipeline, Spanish pilot ([idiomatic/grammar/](../idiomatic/grammar/)): Gemini-generated one-form-per-card conjugation cloze items, verified against the vendored Jehle verb DB (wrong/unverifiable forms are persisted as `rejected`, never shipped); rolling `kind='grammar'` apkg per language delivered through the existing add-on path; admin endpoints `grammar-generate/status/stats/rebuild`; `grammar_items` table; deterministic tests in [tests/test_grammar.py](../tests/test_grammar.py).
- `docs/` structure: feature inventory ([docs/FEATURES.md](FEATURES.md)) and this changelog.
- Grammar-exercise strategy document ([docs/GRAMMAR_STRATEGY.md](GRAMMAR_STRATEGY.md)) — research-backed plan for LLM-generated, personalized grammar drills across es/pt/fr/it/de.
- Research annex ([docs/research/](research/)): four commissioned reports (community wisdom, data sources & licenses, SLA pedagogy, AnkiDroid/genanki tech) grounding the grammar strategy.

---

## [2026-07-27]

### Added
- Worker-side disk janitor: media_stage sweep + delivered-apkg reaper (`idiomatic/worker.py`).
- Queue auto-expiry: queued news older than 7 days expires (cron).

### Changed
- TTS: ElevenLabs turbo v2.5 primary; Gemini TTS preview demoted to fallback.
